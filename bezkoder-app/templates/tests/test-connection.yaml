apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bezkoder-app.fullname" . }}-test-connection"
  labels:
    {{- include "bezkoder-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bezkoder-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
